# Optional: API origin only — do not add /api (the client appends it).
# Dev usually leaves this unset and uses the Vite proxy for /api.
# VITE_API_URL=http://localhost:5001
# Dev only: where Vite proxies /api and /uploads (default http://127.0.0.1:5001)
# VITE_DEV_API_ORIGIN=http://127.0.0.1:5001
